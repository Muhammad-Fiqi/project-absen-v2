# Windows local dev env (used by Prisma CLI by default)
# Prisma SQLite URL MUST start with `file:`
DATABASE_URL="file:db/custom.db"

# Secrets (override in production)
AUTH_SECRET="pte-attendance-auth-key-2024"
ATTENDANCE_SECRET="pte-attendance-secret-key-2024-secure"

